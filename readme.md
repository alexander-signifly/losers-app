## [Vue template](https://cli.vuejs.org)

### Docker

``` bash
# Change container_name in docker-compose.yml

# start app in development mode
$ docker-compose up

# build app
$ docker-compose run --rm app yarn build
```
